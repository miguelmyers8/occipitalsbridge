<%= form_for([@forum, @forum.topics.build]) do |f| %>


  <div class="form-inputs">
    <%= f.text_area :name %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %><br>
  </div>
<% end %>
